import { COLORS } from '../constants/colors';

export const themes = {
    theme1: {
        colors: COLORS.theme1,
        borderRadius: '30px',
        shadow: COLORS.shadow,
        sideText: COLORS.theme1.sub, // 사이드바 텍스트 색상
    },
    theme2: {
        colors: COLORS.theme2,
        borderRadius: '30px',
        shadow: COLORS.shadow,
        sideText: COLORS.theme2.sub,
    },
    theme3: {
        colors: COLORS.theme3,
        borderRadius: '30px',
        shadow: COLORS.shadow,
        sideText: COLORS.theme3.sub,
    },
};